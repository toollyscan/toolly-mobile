# Benchmark Evidence

No benchmark run is checked in yet.

Create one immutable directory per run:

```text
<benchmark-id>/<run-id>/run.json
<benchmark-id>/<run-id>/measurements.jsonl
```

Templates, screenshots and hand-written summaries are not accepted as raw evidence. See [Benchmark Evidence Format](../../docs/quality/BENCHMARK_EVIDENCE.md).
