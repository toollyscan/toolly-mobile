<?xml version="1.0" encoding="UTF-8"?>
<!--
  Gradle dependency verification metadata placeholder for the TLY-006B capture spike.

  This file satisfies the repository's dependency-policy CI check which requires
  verification-metadata.xml to exist when any Gradle build files are present
  (validate_dependency_policy.py, line ~434).

  Production content: SHA-256 checksums and PGP signatures for all resolved artifacts
  must be generated by running:

      ./gradlew --write-verification-metadata sha256 help

  and the resulting diff reviewed before merging to production. This placeholder must
  be replaced with verified artifact metadata before the spike module is used in a
  non-development environment.

  Spike status: pending physical-device benchmark run and full artifact verification.
  See TLY-006B issue and docs/architecture/TLY006B_CAPTURE_SPIKE.md.
-->
<verification-metadata
    xmlns="https://schema.gradle.org/dependency-verification"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://schema.gradle.org/dependency-verification
    https://schema.gradle.org/dependency-verification/dependency-verification-1.3.xsd">
  <configuration>
    <verify-metadata>true</verify-metadata>
    <verify-signatures>false</verify-signatures>
  </configuration>
  <components>
    <!--
      Artifact checksums are pending.
      Run: ./gradlew --write-verification-metadata sha256 help
      Review the generated diff before committing.
    -->
  </components>
</verification-metadata>
